<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>New Product</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css"
    />
  </head>
  <body>
    <main class="container m-5">
      <h1 class="is-size-1 has-text-centered">Add new Prodcut</h1>
      <section class="section">
        <form action="/products" method="POST">
          <div class="field">
            <label for="name" class="label">Product Name</label>
            <div class="control">
              <input
                type="text"
                name="name"
                id="name"
                class="input"
                placeholder="Product Name"
              />
            </div>
          </div>

          <div class="field">
            <label for="price" class="label">Product Price</label>
            <div class="control">
              <input
                type="number"
                name="price"
                id="price"
                class="input"
                placeholder="Product Price (Unit)"
              />
            </div>
          </div>

          <div class="field">
            <label for="category" class="label">Product Category</label>
            <div class="control">
              <div class="select">
                <select name="category" id="category">
                  <% for(let category of categories) {%>
                  <option value="<%= category %> "><%= category %></option>
                  <% } %>
                </select>
              </div>
            </div>
          </div>

          <div class="field is-grouped">
            <div class="control">
              <button class="button is-link">Submit</button>
            </div>
            <div class="control">
              <a href="/products" class="button is-danger is-light">Cancle</a>
            </div>
          </div>
        </form>
      </section>
    </main>
  </body>
</html>
